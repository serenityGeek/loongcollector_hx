# LoongCollector 项目解读报告

> 本报告基于对 `loongcollector` 仓库的源码分析（C++ 核心、Go 插件、构建系统、配置与部署）完整解读，帮助开发者快速建立对项目的认知地图。

---

## 一、项目定位与技术栈

### 1.1 项目定位

LoongCollector 是阿里巴巴开源的**高性能、轻量级可观测数据采集器**，前身为 iLogtail，已在阿里内部打磨 15 年以上，支撑双 11 等极端场景下数千万级部署与每日数百 PB 级数据。它是 LoongSuite 可观测套件的核心节点侧采集代理（node agent），定位为**统一的日志 / 指标 / Trace / Event / Profile 采集器**，而非单一日志工具。

### 1.2 技术栈

| 层次 | 技术选型 |
|------|----------|
| 核心语言 | **C++17**（热路径、零拷贝、低资源占用）+ **Go 1.23**（插件生态扩展） |
| 构建 | CMake（core）+ Go Modules（plugin）+ 顶层 Makefile 串联 |
| 跨语言 | CGo `c-shared` 模式 + `dlopen/dlsym` 动态加载 |
| 协议 | Protobuf（SLS、ConfigServer）、gRPC、HTTP、OpenTelemetry OTLP |
| 关键三方库（C++） | Boost、Protobuf、gRPC、jsoncpp、yaml-cpp、spdlog、LZ4/zstd、curl+OpenSSL、leveldb、re2、gperftools、gtest |
| 关键三方库（Go） | sarama(Kafka)、clickhouse-go、pulsar-client-go、elasticsearch、k8s client-go、docker、seelog、xxhash、regexp2 |
| 平台 | Linux amd64/arm64、Windows amd64（eBPF 模块仅 Linux） |

### 1.3 差异化定位

- 对比 **Filebeat / FluentBit**：单线程吞吐可达 546 MB/s，资源占用显著更低，正则解析性能尤其突出。
- 对比 **Fluentd**：C++ 原生核心避免了 Ruby/Go 运行时开销，更适合边缘高密度部署。
- 对比 **Vector**：多租户 Pipeline 隔离 + AIMD 自适应流控更成熟，原生集成 eBPF。
- 对比 **OpenTelemetry Collector**：更侧重采集侧极致性能（内存池、零拷贝序列化），同时保留 OTLP 协议兼容。

---

## 二、整体架构设计

### 2.1 双语言架构理念

```
+-------------------------------------------------------------+
|                   Loongcollector 进程                        |
|                                                             |
|   +----------------+        CGo        +----------------+   |
|   |    C++ Core    |  <-------------→  |   Go Plugins   |   |
|   | (high-speed)   |   dlopen/dlsym    |  (ecosystem)   |   |
|   +----------------+                   +----------------+   |
|   file_server / ebpf                    input_docker        |
|   prometheus                            flusher_kafka       |
|   host_monitor                          processor_grok      |
|   collection_pipeline                   flusher_es/loki...  |
+-------------------------------------------------------------+
                         ↕ 配置下发 / 数据上行
              +--------------------------------+
              | ConfigServer / SLS / Kafka ... |
              +--------------------------------+
```

- **C++ 核心**：专注高性能热路径（文件读取、多行切分、SPL 处理、批量化发送），追求低 CPU/内存占用。
- **Go 插件**：承担丰富但非极致性能的生态（各类下游 Flusher、扩展 Processor、服务发现）。
- 两者通过 CGo 双向调用：C++ 端 `core/go_pipeline/` 作为胶水层，Go 端 `pluginmanager/` 作为流水线管理器。

### 2.2 Pipeline 抽象

整个数据流的一等公民是 **CollectionPipeline**，由 `inputs → processors → aggregators → flushers` 四段构成，每条 Pipeline 拥有独立的队列、批处理器、限流器、路由与序列化器。Pipeline 之间完全隔离，互不影响。另有 `TaskPipeline` 处理一次性任务。

### 2.3 控制面与数据面分离

- **控制面**：独立的 `config_server/` 定义 gRPC/Proto 契约，负责配置下发、版本管理、Agent 心跳。
- **数据面**：LoongCollector 本体负责执行配置。也可通过 SLS 控制台、K8s Operator 下发配置。

---

## 三、模块划分与职责

### 3.1 顶层目录

| 目录 | 职责 |
|------|------|
| [core/](core/) | C++ 核心，日志/指标/Trace 高性能采集引擎 |
| [pluginmanager/](pluginmanager/) | Go 插件运行时，管理插件生命周期与调度 |
| [plugin_main/](plugin_main/) | CGo 导出入口，Go 侧对 C++ 暴露的 ABI |
| [plugins/](plugins/) | Go 插件实现（input、processor、aggregator、flusher、extension） |
| [pkg/](pkg/) | Go 公共包（pipeline 接口、models、protocol、helper 等） |
| [config_server/](config_server/) | 控制面协议定义（v1/v2 proto） |
| [scripts/](scripts/) | 构建 / 部署 / 运维脚本 |
| [docker/](docker/) | 多类 Dockerfile（生产、单测、E2E、构建） |
| [k8s_templates/](k8s_templates/) | Kubernetes DaemonSet 部署模板 |
| [example_config/](example_config/) | 配置示例（快速开始、Docker、各类数据管道） |
| [external/](external/) | 企业版 / 外部扩展插件接入点 |
| [test/](test/) | E2E 集成测试 |
| [changes/](changes/) | Release notes 片段 |

### 3.2 C++ 核心（core/）模块划分

| 模块 | 职责 |
|------|------|
| [application/](core/application/) | 进程级编排者 `Application` 单例，负责 Init / Start / Exit |
| [app_config/](core/app_config/) | `AppConfig` 加载 `loongcollector_config.json` 全局参数 |
| [config/](core/config/) | 采集配置抽象：`CollectionConfig`、`PipelineConfig`、`TaskConfig`、`InstanceConfig`，以及 watcher / provider / feedbacker |
| [collection_pipeline/](core/collection_pipeline/) | **采集流水线核心**：`CollectionPipeline` / `CollectionPipelineManager`，以及 batch / limiter / queue / route / serializer |
| [plugin/](core/plugin/) | C++ 原生插件实现（input / processor / flusher / task） |
| [runner/](core/runner/) | 执行线程：`InputRunner` / `ProcessorRunner` / `FlusherRunner`，`sink/http` 异步 HTTP 发送 |
| [file_server/](core/file_server/) | 文件采集主力：目录发现、inotify/kqueue 监听、事件派发、Reader、Checkpoint、容器路径处理 |
| [go_pipeline/](core/go_pipeline/) | **与 Go pluginmanager 的桥接层**，`LogtailPlugin` / `LogtailPluginAdapter` |
| [container_manager/](core/container_manager/) | 容器元信息查询与缓存 |
| [ebpf/](core/ebpf/) | eBPF 可观测采集（network / process / file / security） |
| [host_monitor/](core/host_monitor/) | 主机 CPU/内存/磁盘/网络/进程指标 |
| [prometheus/](core/prometheus/) | Prometheus scrape（schedulers、labels 处理） |
| [forward/](core/forward/) | gRPC/OTLP 数据转发 |
| [monitor/](core/monitor/) | 自监控：`Monitor`、`SelfMonitorServer`、`MetricManager`、`AlarmManager`、`profile_sender` |
| [models/](core/models/) | 统一事件模型（详见下文第五节） |
| [parser/](core/parser/) | 解析器 |
| [logger/](core/logger/) | 基于 spdlog 的日志门面 |
| [common/](core/common/) | 通用工具 |
| [metadata/](core/metadata/) | 元数据管理 |
| [protobuf/](core/protobuf/) | Protobuf 生成代码与封装 |
| [provider/](core/provider/) | 扩展点 |
| [task_pipeline/](core/task_pipeline/) | 一次性任务流水线 |
| [unittest/](core/unittest/) | 单元测试（gtest） |

### 3.3 Go 插件（plugins/）分类

| 类型 | 代表插件 |
|------|---------|
| **input** | `docker`、`kubernetesmeta_v1/v2`、`mysqlbinlog`、`redis`、`journal`、`syslog`、`httpserver`、`prometheus`、`opentelemetry`、`skywalking`、`kafka`、`jmxfetch`、`telegraf`、`snmp`、`process`、`canal`、`mqtt` 等（文件采集在 C++） |
| **processor** | `regex`、`json`、`grok`、`split`、`csv`、`addfields`、`rename`、`drop`、`filter`、`desensitize`、`encrypt`、`base64`、`geoip`、`gotime`、`strptime`、`ratelimit`、`appender`、`cloudmeta`、`logtosls_metric`、`otel` 等 |
| **aggregator** | `aggregator_default`、`context`、`logstore_router`、`metadata_group`、`shardhash`、`skywalking`、`opentelemetry` |
| **flusher** | `sls`、`kafka/kafkav2`、`clickhouse`、`doris`、`elasticsearch`、`pulsar`、`loki`、`prometheus`、`opentelemetry`、`grpc`、`http`、`stdout`、`checker`、`statistics`、`shsnc` |
| **extension** | decoder、encoder、authenticator 等扩展点 |

---

## 四、入口与执行机制

### 4.1 C++ 启动链

`core/logtail.cpp` 是 Linux 平台进程入口（Windows 对应 `logtail_windows.cpp`），职责薄：解析参数、初始化 gflags、安装信号，然后把控制权交给 `Application` 单例。

```
main(argc, argv)
  └─ ParseCommandLineFlags()
     └─ do_worker_process()
        ├─ CreateAgentDir()
        ├─ Logger::Instance().InitGlobalLoggers()  // spdlog
        ├─ sigaction(SIGTERM/SIGINT, HandleSigtermSignal)
        ├─ Application::GetInstance()->Init()
        └─ Application::GetInstance()->Start()      // 主循环
```

**当前 `logtail.cpp` 的扩展**（用户改动）：新增了一套 **JNI 接入层**（`JNI_OnLoad` / `loogcollectorStart` / `SncAgentSendData`），把整个 LoongCollector 以动态库形式嵌入 JVM 宿主进程（SncAgent），即同时支持"独立二进制"与"JNI so 库"两种启动形态。

### 4.2 `Application::Init()` 内部顺序

1. 加载 `AppConfig`
2. 启动 `EnterpriseConfigProvider`
3. 初始化 `Monitor` / `AlarmManager` / `PluginRegistry` / `InputFeedbackInterfaceRegistry` / `ExactlyOnceQueueManager`
4. 启动 `HttpSink`、`FlusherRunner`、`ProcessorRunner`
5. 加载 `LogtailPlugin`（Go 插件容器）

### 4.3 `Application::Start()` 主循环

每秒循环：
- `CollectionPipelineManager::UpdatePipelines()` 处理配置变更
- `ConfigWatcher` 轮询新配置
- 容器差异检查
- `DeleteHandlers()` 清理僵尸句柄
- `CheckCriticalCondition` 超时自愈（必要时自杀重启）
- `sleep_for(1s)`

### 4.4 Go 侧入口（plugin_main/）

Go 以 `c-shared` 模式编译为 `libPluginBase.so`，通过 `//export` 注释导出 CGo 符号：

| 导出函数 | 作用 |
|---------|------|
| `InitPluginBase(V2)` | 基础初始化（`sync.Once` 幂等） |
| `LoadGlobalConfig(jsonStr)` | 加载全局配置并初始化 logger |
| `LoadPipeline(project, logstore, configName, logstoreKey, jsonStr)` | 下发单条流水线配置 |
| `UnloadPipeline(configName)` | 卸载流水线 |
| `Start / Stop / StopAllPipelines` | 生命周期控制 |
| `ProcessLog / ProcessLogGroup` | C++ 把原始日志透传给 Go 处理链 |
| `CtlCmd` | 控制命令（容器路径发现等） |
| `GetContainerMeta` | Go 回传容器元信息给 C++ |
| `GetGoMetrics` | Go 回传自监控指标给 C++ |

---

## 五、核心业务流程（按执行顺序）

```
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │  配置加载    │ -> │  输入采集    │ -> │  解析处理    │ -> │  聚合批次    │ -> │  输出发送    │
  │ ConfigWatch │    │ Input Plugin│    │ Processor   │    │ Aggregator  │    │  Flusher    │
  └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
                       (C++ / Go)        (C++ / Go)          (C++ / Go)        (C++ / Go)
```

### 步骤 1：配置加载

- `ConfigWatcher` 监听 `config/local` 目录与远程 `ConfigProvider` 下发的 JSON
- 生成 `ConfigDiff`（added / modified / removed）
- `CollectionPipelineManager::UpdatePipelines(ConfigDiff)` 原子化地创建/替换/删除 Pipeline

### 步骤 2：流水线构建

- 按 `inputs → processors → aggregators → flushers` 顺序调用 `PluginRegistry` 生成各 `*Instance`
- 建立 `queue/`（处理队列、发送队列）与 `route/`（多路分发）
- 若包含 Go 插件，调用 `LogtailPlugin::LoadPipeline` 把配置透传给 Go 侧 `pluginmanager.LoadLogstoreConfig`

### 步骤 3：数据源采集

- **C++ 原生 input**：`input_file`（`FileServer` 驱动）、`input_ebpf`、`input_prometheus`、`input_host_monitor` 等产出 `PipelineEventGroup`
- **Go input**：通过 `LogtailPlugin` 反向灌入，容器日志、Docker、K8s Meta 发现等走此路径

### 步骤 4：处理

- 事件进入 `ProcessQueue`
- `ProcessorRunner` 工作线程出队，按顺序执行 `ProcessorInstance::Process`（正则、解析、标签、采样、限流）
- 若配置了 Go Processor 扩展（grok / geoip / desensitize 等），通过 CGo 移交 Go 侧

### 步骤 5：聚合

- C++ 侧 `batch/` 层按 shardhash、metadata、logstore 等维度聚合
- Go 侧 `aggregator_default` 按 logstore 聚合，其它聚合器支持特殊协议（skywalking、opentelemetry）

### 步骤 6：输出

- 处理结果入 `SenderQueue`
- `FlusherRunner` 调度 `FlusherInstance::Send`
- 通过 `HttpSink` 异步发送；失败则写入 `DiskBufferWriter` 落盘重试
- `limiter/` 做速率控制（AIMD 自适应并发）
- Go Flusher（Kafka、ES、Loki、ClickHouse、Doris、Pulsar、OTLP、HTTP）直接在 Go 内完成远端发送

---

## 六、数据流转与跨语言交互

### 6.1 C++ ↔ Go 数据传递

```
C++ Pipeline                          Go Pipeline
     │                                     │
     │    ProcessLog(LogGroup pb bytes)    │
     ├────────────────────────────────────>│
     │    (protobuf 序列化 LogGroup)        │
     │                                     │
     │<────────────────────────────────────┤
     │    数据回 Flusher / GetContainerMeta │
     │                                     │
```

- **C++ → Go**：`ProcessLogsFun` 把 `sls_logs::LogGroup`（protobuf 序列化字节缓冲）传给 Go 做二次处理
- **Go → C++**：Go 插件把采集的数据封装回 `LogGroup` / `PipelineEventGroup` 再交给 `FlusherSLS` 或 C++ 流水线
- V2 协议：`models.PipelineGroupEvents`（去 protobuf 化内存表示），减少拷贝开销

### 6.2 桥接实现机制

`core/go_pipeline/LogtailPluginAdapter.cpp` 采用 **CGo 导出 + dlsym 动态加载**：

1. C++ `dlopen("libGoPluginAdapter.so")` 加载 Go 共享库
2. 用 `dlsym` 绑定 Go 侧 `//export` 的符号（`LoadGlobalConfig` / `LoadPipeline` / `ProcessLogs` 等）
3. `extern "C"` 声明函数指针类型（`ProcessLogsFun` / `ProcessLogGroupFun` / `GetContainerMetaFun`）
4. `LoadPluginBase` 把 C++ 回调（写日志、发送到 SLS、查询容器元数据）注册到 Go 侧

> **注意**：项目中还存在 JNI 接口（`com_shsnc_agent_ivory_plugin_loongcollector_LoongCollectorProcessor.h`），这是用户分支额外添加的"作为 so 被 Java 加载"路径，与主线 CGo 机制并存。

---

## 七、关键函数 / 类作用

### 7.1 C++ 核心类

| 类/单例 | 作用 |
|---------|------|
| `Application` | 进程级编排者（单例），持有 `mInstanceId`、`mStartTime`、`mSigTermSignalFlag`，提供 `Init/Start/Exit` |
| `AppConfig` | 全局配置 |
| `CollectionPipelineManager` | 按 `configName` 管理所有流水线生命周期，`UpdatePipelines(ConfigDiff)` 为热更新入口 |
| `CollectionPipeline` | 一条流水线运行实例，聚合 `InputInstance` / `ProcessorInstance` / `FlusherInstance` |
| `ConfigWatcher` / `PipelineConfigWatcher` | 扫描 config 目录并产出 `ConfigDiff` |
| `PluginRegistry` | C++ 插件工厂注册表 |
| `FileServer` | 文件采集总控（Pause/Resume、轮询与 inotify 协作、checkpoint） |
| `LogtailPlugin` | Go 插件容器的 C++ 代理 |
| `ProcessorRunner` / `FlusherRunner` / `HttpSink` | 处理与输出的执行线程池 |
| `Monitor` / `AlarmManager` | 自监控与告警 |

### 7.2 Go 核心类型

| 类型 | 作用 |
|------|------|
| `LogtailConfig map[string]*LogstoreConfig` | 全局流水线注册表（configName → 实例） |
| `LogstoreConfig` | 一条完整流水线，持有 `Context` / `GlobalConfig` / `PluginRunner` |
| `PluginRunner` 接口 | 定义 `Init / AddPlugin / ReceiveRawLog / ReceiveLogGroup / Run / Stop` |
| `PluginRunnerV1/V2` | 两种 wire format 的实现（旧 `protocol.Log` vs 新 `models.PipelineGroupEvents`） |
| `plugin_wrapper_*` | MetricInput / ServiceInput / Processor / Aggregator / Flusher 的运行时包装器（goroutine、自监控、panicRecover） |
| `CheckPointManager` | boltdb 实现的检查点 |
| `ContainerConfigManager` | K8s/容器路径发现 |

### 7.3 数据模型（core/models/）

| 类型 | 作用 |
|------|------|
| `PipelineEvent` | 事件基类，时间戳 + 类型枚举 |
| `LogEvent` | 键值字段集合，日志场景 |
| `MetricEvent` + `MetricValue` | 名称、标签、Untyped/Gauge/Counter/Distribution 等值类型 |
| `SpanEvent` | trace span（trace/span id、kind、status、events、links） |
| `RawEvent` | 原始字节事件，未解析阶段使用 |
| `PipelineEventGroup` | 一批共享元数据/标签的事件，流水线间传递基本单元；内部 `SourceBuffer` 做零拷贝字符串管理 |
| `PipelineEventPtr` | 类型擦除的事件句柄，便于容器存放多种派生类型 |
| `EventPool` | 对象池，降低事件频繁分配释放开销 |

---

## 八、依赖关系

### 8.1 C++ 依赖（core/dependencies.cmake）

| 类别 | 依赖 |
|------|------|
| 序列化/RPC | Protobuf、gRPC |
| 通用 | Boost（regex / filesystem / system / thread）、jsoncpp、yaml-cpp |
| 网络/加密 | curl、OpenSSL |
| 压缩 | LZ4、zstd、zlib |
| 日志 | spdlog、fmt |
| 存储 | leveldb（checkpoint） |
| 正则/哈希 | re2、cityhash、xxhash |
| 内存/诊断 | gperftools（tcmalloc + heap profiler）、libunwind、breakpad |
| 测试 | gtest、gflags |
| 可观测特化 | coolbpf（eBPF） |
| Windows 追加 | ws2_32、iphlpapi |
| Linux 默认 | 静态链接 libstdc++/libgcc，保留 dl/rt/pthread |

### 8.2 Go 依赖（go.mod）

module 为 `github.com/alibaba/ilogtail`，Go 1.23，通过 `replace` 把子模块 `pkg/` 指向本地目录。

| 类别 | 依赖 |
|------|------|
| 消息队列/存储 | sarama（Kafka）、pulsar-client-go、clickhouse-go、go-doris-sdk、go-elasticsearch、go-mssqldb、go-mysql、go-lumber |
| 容器/编排 | docker、k8s.io/* 系列 |
| 可观测 | beats、opentelemetry、VictoriaMetrics |
| 解析/编解码 | buger/jsonparser、regexp2、xxhash、seelog |
| 其它 | paho.mqtt、coreos/go-systemd、fnmatch |

### 8.3 External 目录

`external/` 放置企业版或外部扩展插件接入代码，通过 `PLUGINS_CONFIG_FILE=plugins.yml,external_plugins.yml` 被 `scripts/import_plugins.sh` 聚合编译进 `plugin_main`。

---

## 九、构建系统

### 9.1 顶层 Makefile 关键 target

版本 `VERSION=3.3.2`，自动识别 linux/darwin/windows 与 amd64/arm64。

| Target | 作用 |
|--------|------|
| `all` | 默认目标，串联 core + plugin 构建并整合产物 |
| `core` | 通过 `scripts/gen_build_scripts.sh` 生成并调用 C++ 构建 |
| `plugin` / `plugin_local` | 通过 `scripts/plugin_build.sh` 编译 Go 插件层 |
| `dist` | 打包 `output/` 为 `loongcollector-<VER>.linux-<ARCH>.tar.gz` |
| `docker` / `multi-arch-docker` | 基于 dist 产物构建镜像 |
| `clean` / `check-dependency-licenses` / `solib` | 辅助 |

### 9.2 C++ 与 Go 协同构建

- Go 通过 `go build -buildmode=c-shared` 产出 `libPluginBase.so`（基础插件框架）
- C++ 构建 `libGoPluginAdapter.so` 作为 adapter，主体产物为 `libloongcollector.so`、`libcoolbpf.so`、`libeBPFDriver.so`
- 运行时 **C++ 主导**：通过 adapter `dlopen` 加载 Go .so，调用 Go 函数；Go 侧仅以 `//export` 提供 ABI
- x86_64 Linux 使用 `-Wl,--wrap=memcpy` 兼容旧 glibc

### 9.3 关键脚本（scripts/）

| 脚本 | 作用 |
|------|------|
| `plugin_build.sh` | Go 层构建主脚本，可选 `c-shared` / 独立可执行 |
| `import_plugins.sh` | 按 `plugins.yml,external_plugins.yml` 自动生成插件注册代码 |
| `download_dependencies.sh` | 下载 C++ 三方源码 |
| `download_ebpflib.sh` | 下载 eBPF 库 |
| `docker_build.sh` | 容器镜像构建 |
| `dist.sh` | 产物打包 |
| `loongcollector_control.sh` | 运行期 start / stop / restart / start_and_block |
| `run_core_ut.sh` / `e2e.sh` | 单测与 E2E |

---

## 十、部署与运行方式

### 10.1 运行时拓扑

| 部署形态 | 适用场景 |
|---------|---------|
| **DaemonSet**（主流 K8s） | 每节点一实例，挂载 `/var/log`、`/var/run` 采集节点所有容器 |
| **Sidecar** | 与业务容器共享 PID/网络命名空间，隔离性要求高 |
| **独立进程** | systemd 守护进程，物理机/VM |
| **Docker 单机** | `make docker` 构建 `alibaba/loongcollector:x.y.z` |
| **JNI so 嵌入 JVM**（本分支扩展） | 作为 Java 宿主进程的插件运行 |

### 10.2 Docker 镜像

- `Dockerfile_production`：两阶段构建，运行阶段基于 `centos:7.9.2009`，放入 `/usr/local/loongcollector/`，默认 `CMD` 为 `loongcollector_control.sh start_and_block`，暴露 `HTTP_PROBE_PORT=7953`
- 其它变体：`Dockerfile_coreut`（core 单测）、`Dockerfile_goc`（Go 覆盖率）、`Dockerfile_build_*`、`Dockerfile_e2e`

### 10.3 配置管理

- **全局配置**：`loongcollector_config.json`（JSON，实例级参数）
- **采集配置**：`config/*.yaml`（YAML，Pipeline DSL）
- **示例**：[example_config/quick_start/config/file_simple.yaml](example_config/quick_start/config/file_simple.yaml) 展示最小采集管道（`input_file → flusher_stdout`）
- **动态下发**：`config_server/protocol/v1,v2` 定义 gRPC/Proto 契约

### 10.4 进程模型

**单进程多线程**：主进程 `loongcollector` 内嵌 C++ 核心（file_server、ebpf driver、host_monitor、collection_pipeline、runner、task_pipeline）并通过 `go_pipeline` adapter 加载 Go 插件 so；`start_and_block` 由控制脚本前台阻塞供容器 PID 1 使用。

### 10.5 运维要点

- PID 文件：`run/loongcollector.pid`
- 存活探测：`HTTP_PROBE_PORT=7953`，`LIVENESS_CHECK_INTERVAL=3s`
- 运行目录：`/usr/local/loongcollector/`（conf、log、data、run）

### 10.6 测试与 CI

- **C++ 单元测试**：`core/unittest/`，gtest，`BUILD_LOGTAIL_UT=ON`，入口 `scripts/run_core_ut.sh`
- **Go 单测**：包内 `*_test.go`
- **E2E**：`test/`，入口 `scripts/e2e.sh`
- **CI 工作流**（`.github/workflows/`）：
  - `build-linux.yaml` / `build-windows.yaml`
  - `check-compatibility.yaml` / `static-check.yaml`
  - `e2e.yaml`
  - `release-edge.yaml` / `auto-release.yaml`

---

## 十一、关键设计亮点

1. **多租户 Pipeline 隔离**：每条 Pipeline 独立高低水位反馈队列与资源预算，单租户拥塞不影响他人。
2. **Checkpoint 断点续传**：`core/file_server/` 持久化文件 inode+offset；`pluginmanager/checkpoint_manager.go` 用 boltdb 管理插件级 checkpoint，重启后无损续采。
3. **反向传压**：高低水位队列 + **AIMD 自适应并发**（每目的端自动加减并发度，快速失败、平滑恢复）+ 优先级轮询调度。
4. **动态配置热更新**：ConfigManager 周期 diff，Pipeline 增删原子操作，老 Pipeline 优雅 drain 后回收。
5. **零拷贝事件模型**：`PipelineEventGroup` + `SourceBuffer` 让字符串字段全部指向共享缓冲区，大批量事件处理无额外拷贝。
6. **双运行时插件生态**：C++ 热路径保证极限性能，Go 扩展保证生态丰富度，二者无缝组合。
7. **多维度可观测**：原生集成 eBPF（network / process / file / security），不依赖用户态 hook。

---

## 十二、核心数据流图（端到端）

```
 ┌─────────────────────────────────────────────────────────────────────────────┐
 │                               控制面                                          │
 │    ConfigServer / SLS Console / K8s Operator                                 │
 │              │ gRPC 下发配置（JSON / YAML）                                    │
 └──────────────┼───────────────────────────────────────────────────────────────┘
                ▼
 ┌─────────────────────────────────────────────────────────────────────────────┐
 │                         LoongCollector 进程                                   │
 │                                                                               │
 │  ConfigWatcher ──ConfigDiff──▶ CollectionPipelineManager                     │
 │                                       │                                       │
 │                                       ▼ 构建/替换 Pipeline                     │
 │  ┌────────────┐   ┌────────────┐   ┌──────────────┐   ┌──────────┐           │
 │  │   Input    │──▶│ Processor  │──▶│  Aggregator  │──▶│ Flusher  │──▶ 后端   │
 │  └────────────┘   └────────────┘   └──────────────┘   └──────────┘           │
 │    file_server      regex/json        batch/shardhash    SLS/Kafka/ES        │
 │    ebpf/prom        grok/geoip        metadata_group     Loki/OTLP/HTTP      │
 │    host_monitor     desensitize       ...                ...                 │
 │         ▲                                                    │               │
 │         │  PipelineEventGroup（零拷贝）                       │ DiskBuffer    │
 │         │                                                    ▼ 失败重试       │
 │  ┌─────────────────────────────────────────────────┐    ┌──────────┐        │
 │  │  CGo Bridge (go_pipeline / pluginmanager)       │    │  磁盘     │        │
 │  │  dlopen libPluginBase.so / libGoPluginAdapter.so│    └──────────┘        │
 │  └─────────────────────────────────────────────────┘                         │
 │                                                                               │
 │  Monitor / AlarmManager / CheckpointManager / SelfMonitorServer              │
 └──────────────────────────────────────────────────────────────────────────────┘
                │ 自监控指标
                ▼
         Prometheus / 自有监控
```

---

## 十三、阅读建议（如何继续深入）

1. **C++ 入口**：[core/logtail.cpp](core/logtail.cpp) → [core/application/Application.cpp](core/application/) → [core/collection_pipeline/CollectionPipelineManager.cpp](core/collection_pipeline/)
2. **Go 入口**：[plugin_main/plugin_export.go](plugin_main/plugin_export.go) → [pluginmanager/plugin_manager.go](pluginmanager/plugin_manager.go) → [pluginmanager/logstore_config.go](pluginmanager/logstore_config.go)
3. **插件接口**：[pkg/pipeline/](pkg/pipeline/) 下的 `input.go` / `processor.go` / `aggregator.go` / `flusher.go`
4. **跨语言桥**：[core/go_pipeline/](core/go_pipeline/) 的 `LogtailPlugin.cpp` 与 `LogtailPluginAdapter.cpp`
5. **一个端到端示例**：从 [example_config/quick_start/config/file_simple.yaml](example_config/quick_start/config/file_simple.yaml) 开始，跟踪 `input_file` 配置如何被加载、文件如何被采集、日志如何被发送。
6. **构建实操**：照 [compile.txt](compile.txt) 的步骤本地跑一次完整构建，理解各产物关系。

---

*报告生成日期：2026-05-08*
